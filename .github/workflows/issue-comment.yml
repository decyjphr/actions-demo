name: Issue Comment
on:
  issues:
    types: [opened]
    
jobs:
  hello:
    runs-on: ubuntu-latest
    name: Greet 
    steps:
      - name: javascript
        uses: ./.github/actions/hello-world
      
  
  
